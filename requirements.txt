# El harness usa solo la stdlib de Python (3.11+). No requiere dependencias para el motor `mock`.
# Estas son para levantar los TARGETS de ejemplo y correr los tests.
flask>=3.0
jinja2>=3.1
pytest>=8.0
